spring:
  jpa:
    show-sql: true
  application:
    name: EmployeeAddress
server:
  port: 8090
eureka:
  instance:
    lease-renewal-interval-in-seconds: 1
    lease-expiration-duration-in-seconds: 2
      