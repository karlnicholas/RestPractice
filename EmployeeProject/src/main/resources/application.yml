spring:
  jpa:
    show-sql: true
  application:
    name: EmployeeProject
server:
  port: 8092
eureka:
  instance:
    lease-renewal-interval-in-seconds: 1
    lease-expiration-duration-in-seconds: 2
